2. `quinn`(QUIC)
3. `rustls`(TLS 설정)


# CA키 만들기
```
openssl genrsa -out ca.key 4096
openssl req -x509 -new -nodes -key ca.key -sha256 -days 3650 \
  -subj "/CN=quic-tunnel-root-ca" \
  -out ca.crt
```
![[Pasted image 20260202214252.png|500]]