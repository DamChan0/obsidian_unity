>TLS (Transport Layer Security)는 네트워크 통신을 암호화하고, 상대를 인증하며, 무결성을 보장하는 프로토콜이다.

TLS를 사용하여 중간에서 데이터를 가로채도 내용을 알기 어렵게 하는 역할을한다

|기능|설명|
|---|---|
|🔐 암호화 (Confidentiality)|제3자가 내용 못 봄|
|🧾 인증 (Authentication)|상대가 진짜인지 확인|
|🧩 무결성 (Integrity)|데이터 위변조 방지|


## 동작 위치

```
Application (HTTP)
        ↓
TLS
        ↓
TCP
        ↓
IP
```

## 동작 단계 

### 1. Handshake 
- 사용할 암호 알고리즘 협상
- 서버 인증서 검증
- (선택적으로) 클라이언트 인증서 검증
- 세션 키 생성
이때 네 코드의 `ServerConfig`가 사용된다.

```
Client → Server: hello (지원 알고리즘 목록)
Server → Client: certificate + 선택된 알고리즘
Client → Server: 인증서 검증 완료 + key 교환
→ 암호화 채널 확립
```

### 2.Application Data 단계

Handshake 끝나면:
- 세션 키 기반 대칭키 암호화 시작
- 실제 데이터 송수신


## 요약 

| 질문                     | 답              |
| ---------------------- | -------------- |
| TLS란?                  | 통신 보안 프로토콜     |
| 무엇을 보장?                | 암호화 + 인증 + 무결성 |
| ServerConfig는?         | TLS 서버 정책 설정   |
| WebPkiClientVerifier는? | 클라이언트 인증 검사기   |

# 🔥 지금 네가 이해해야 할 핵심

- TLS는 암호화 프로토콜이다.
    
- rustls는 TLS 구현체이다.
    
- ServerConfig는 TLS 정책 객체이다.
    
- mTLS는 TLS에서 양방향 인증이다.