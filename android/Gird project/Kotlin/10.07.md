### build.gradle.kts

```kotlin
android {
    // ... 기존 코드들 ...

    // 이 부분을 추가하세요
    buildFeatures {
        viewBinding = true
    }
} 

dependencies { // ... 기존 라이브러리들 ... implementation("io.coil-kt:coil:2.6.0") // Coil 이미지 로더 }
```

### AndroidManifest.xml
```xml
<uses-permission android:name="android.permission.INTERNET" />
```

