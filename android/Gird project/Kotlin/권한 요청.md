## 외부 저장소 권한요청
[[Android 이미지 로딩 문제 해결 가이드#Android 이미지 로딩 문제 해결 가이드#2단계 권한 설정]]

- 권한은 변수로 선언된다 
	- `val permission = mutableListof<String>()`
	- 빈 문자열 배열로 선언한다

### android 12이하
- **외부 저장소 읽기 권한(READ_EXTERNAL_STORAGE)**
	```kotlin
	if (ContextCompat.checkSelfPermission(
	        this, Manifest.permission.READ_EXTERNAL_STORAGE
	    ) != PackageManager.PERMISSION_GRANTED
	) {
	    permissions.add(Manifest.permission.READ_EXTERNAL_STORAGE)
	}
	```
- checkSelfPermission 함수는 매개변수로 오는 내용을 확인한다 
	- 결과가 PackageManager.PERMISSION_GRANTED가 아니면 미허용 상태 
	- 이떄 permissions.add로 부여된 권한을 추가한다.


### android 13이상
`if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.TIRAMISU)`
- 안드로이드 버전 13(tiramisu) 인지 검사한다
- 13 이상은 미디어 별로 권한이 세분화 되었기 때문에 별도의 로직이 필요하다
	- #### 이미지 전용 접근 권한
		```kotlin
		if (ContextCompat.checkSelfPermission(
		        this, Manifest.permission.READ_MEDIA_IMAGES
		    ) != PackageManager.PERMISSION_GRANTED
		) {
		    permissions.add(Manifest.permission.READ_MEDIA_IMAGES)
		}
		```
		- READ_MEDIA_IMAGES 권한이 있는지 확인하고 추가한다 
		- 추가 로직분기 자체는 위와 동일하다

### 실제 권한 요청 로직 
```kotlin
if (permissions.isNotEmpty()) {
    ActivityCompat.requestPermissions(this, permissions.toTypedArray(), 100)
}
```

권한이 없는 것들을 add 헀기 때문에 permission 배열이 비어있지 않다면  `ActivityCompat.requestPermissions` 함수로 권한을 요청하는 팝업을 띄운다.

## 🔎 요약

| 단계  | 내용                                                        |
| --- | --------------------------------------------------------- |
| 1   | `checkSelfPermission()`으로 아직 허용 안 된 권한을 `permissions`에 추가 |
| 2   | `permissions.isNotEmpty()`이면 “허용되지 않은 권한이 있다”로 판단         |
| 3   | `requestPermissions()`는 배열 복사본을 전달할 뿐, 원본 리스트는 변경하지 않음    |
| 4   | 실제 허용/거부 결과는 `onRequestPermissionsResult()` 콜백에서 확인       |